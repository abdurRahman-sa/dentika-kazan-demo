# Служебное macOS
.DS_Store

# Скриншоты ревью — 4.4 МБ, пересоздаются из мокапа за минуту
.impeccable/review/

# Кэш детектора Impeccable, локальный для машины
.impeccable/hook.cache.json

# Мятная версия для Vercel: собирается скриптом scripts/build-mint.mjs при деплое
site/
